/*
	referral view is a tab inside Home. Re-export the Home ReferralTab component
	so screens/ReferralScreen exists and points to the working component.
*/
import { ReferralTab } from '@/app/(tabs)/Home/components/ReferralTab';
export default ReferralTab;
